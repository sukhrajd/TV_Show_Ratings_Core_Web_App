@page
@model TV_Show_Ratings_Core_Web_App.Pages.Subscribers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Subscribers</h1>

<p>
    <a class="btn btn-info" asp-page="Create">+ Add Subscriber</a>
</p>
<table class="table table-striped table-info">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Subscriber[0].SubscriberName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subscriber[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Subscriber) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SubscriberName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a class="btn btn-info" asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a class="btn btn-info" asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a class="btn btn-info" asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
