@page
@model TV_Show_Ratings_Core_Web_App.Pages.TVShows.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TVShow</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TVShow.Id" />
            <div class="form-group">
                <label asp-for="TVShow.TVChannelId" class="control-label"></label>
                <select asp-for="TVShow.TVChannelId" class="form-control" asp-items="ViewBag.TVChannelId"></select>
                <span asp-validation-for="TVShow.TVChannelId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TVShow.ShowName" class="control-label"></label>
                <input asp-for="TVShow.ShowName" class="form-control" />
                <span asp-validation-for="TVShow.ShowName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
